// Placeholder landing page. No business logic -- Milestone 4.4 is
// foundation-only (no groups/expenses features yet, per this milestone's
// constraints).
export function HomePage(): React.JSX.Element {
  return (
    <div>
      <h1>Expense Splitter</h1>
      <p>Frontend foundation running.</p>
    </div>
  );
}
